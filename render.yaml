services:
  - type: web
    name: personal-website
    runtime: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm start
    # No publish directory needed - Next.js runs as Node.js service
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /

